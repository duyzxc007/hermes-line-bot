version: '3.8'

services:
  hermes-bot:
    build: .
    container_name: hermes-bot
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DATA_DIR=/app/data
    env_file:
      - .env
    volumes:
      - ./bot_data:/app/data
